# Database and authentication
MONGODB_URI=
NEXTAUTH_URL=
NEXTAUTH_SECRET=
ADMIN_EMAIL=
ADMIN_PASSWORD=

# SMTP delivery. The inbox recipient can instead be changed in Admin > Settings.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
ENQUIRY_RECEIVER_EMAIL=antony.s8637@gmail.com
# Only set false when your SMTP provider uses a self-signed certificate.
SMTP_TLS_REJECT_UNAUTHORIZED=true
